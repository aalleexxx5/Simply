echo hello does this contain spaces?
set a 10
echo what about this, does a still say *a
echo good, can I then add an ! directly after *a _ !

set a now let's see
set b if everything works with set as well
echo *a
echo *b
set c and if I can add stuff together
set d seamlessly
echo *c *d _ !